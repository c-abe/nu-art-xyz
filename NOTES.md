# 引き継ぎメモ（nu-art.xyz）

セッションをまたぐときに、ここだけ読めば続きから入れるようにしておく。
細かい理由はコード内の日本語コメントと git のコミットメッセージにある。

## いまどうなっているか

- TOP：24点を球に貼り、つかんで360度回す。押すとその1点が正面に回ってから詳細へ
- 詳細：左に戻る／中央に作品と飾ったところ／右に同シリーズ全部と購入ボタン
- モックアップ：190枚（縦の作品5カット、横の作品4カット）
- シリーズ別のサイズと価格
  girls A4 ¥7,000 ／ inside1 B4 ¥6,000 ／ inside2 A4 ¥7,000
  small world B3 ¥10,000 ／ Beautiful Women B3 ¥10,000 ／ Collage A3 ¥18,000

## 次にやること

1. BASE の「かんたん決済」再申請（本人確認の入力があるので NU さん本人が行う）
2. 通ったら `index.html` の `SHOP.live = true` にして購入ボタンを出す
3. 余力があれば BASE の商品画像を、作ったモックアップに差し替える

## 触る前に知っておくこと

- **画像を作り直したら `index.html` の `IMG_VER` を1つ増やす。**
  ファイル名が同じなのでブラウザが古い画像を使い続ける
- **GitHub Desktop に常に出る「2 changed files」はコミットしない。**
  ファイル名の文字コードが違うだけの画像2点。入れると画像が二重になる
- 作業のあとは必ず `node tools/test_detail.mjs`（72項目）
- Push はこの環境からできない。GitHub Desktop から行う

## 試して、やめたこと

同じ道を二度通らないための記録。

- **敷き詰めて並べる TOP** … インスタと同じ見た目になるのでやめた
- **部屋のモックを TOP に並べる** … 背景がうるさく、作品が負けた
- **漂わせる／カーソルの軌跡** … 見比べた結果、球を選んだ（試作は `tools/motion.html`）
- **Mockup.psd（立てかけ）** … 枠が下地に斜めに焼き込まれていて、
  印刷面を 1:√2 に直すと必ずずれる
- **比率の合わない額** … 作品は全40点が 1:√2。合わない額は作品の端を切るので外した

## 道具

    python3 tools/make_frame_scenes.py     # 部屋の写真に A/B判の額を組む（6種）
    python3 tools/make_poster_scenes.py Portrait   # 額なしポスターの下地
    python3 tools/make_mockups.py          # 全作品のモックアップを作り直す
    node tools/test_detail.mjs             # 画面の検査
